{
  "name": "c",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "PC:Chrome": "cypress run --browser chrome --headless",
    "PC:FireFox": "cypress run --browser firefox --headless",
    "MobileChrome": "cypress run --browser chrome --config-file cypress.env.config.js --headed",
    "MobileFireFox": "cypress run --browser firefox --config-file cypress.env.config.js --headed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^11.2.0"
  }
}
